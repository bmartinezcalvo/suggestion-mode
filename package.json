{
  "name": "prototipo-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && touch dist/.nojekyll && cd dist && git init && git add -A && git commit -m 'deploy' && git push -f git@github.com:bmartinezcalvo/suggestion-mode.git main:gh-pages && cd .."
  },
  "dependencies": {
    "vue": "^3.4.0",
    "@wikimedia/codex": "^1.2.0",
    "@wikimedia/codex-design-tokens": "^1.2.0",
    "@wikimedia/codex-icons": "^1.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}

